<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="css/styles.css">
</head>
<body>

<h1><%= title %></h1>

<div class="messages">
<% messages.forEach(function (msg) { %>
  <div class="message"><%= msg %></div>
<% }) %>
</div>

<% if (upload) { %>

    <div>
      <p>Received CIN file: <%= cin_filename %>, size: <%= cin_filesize %> kb</p>
      <p>Created CIN for local trading party: <%= cin_out %></p>
    </div>
    <p>Upload another? Please select the inbound CIN (from other data pool) to process:</p>

<% } %>

<p>Please select the inbound CIN (from other data pool) to process:</p>

<div>
<form method="post" enctype="multipart/form-data">
    <p><span>Step 1</span><input type="file" name="cin" /></p>
    <p><span>Step 2</span><input type="submit" value="Upload" /></p>
</form>
</div>

<% include copyright.ejs %>
</body>
</html>
